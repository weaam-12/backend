spring.application.name=umm-batin-service-management

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# DataSource (TiDB)
spring.datasource.url=jdbc:mysql://gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/users?sslMode=VERIFY_IDENTITY&useSSL=true&requireSSL=true&enabledTLSProtocols=TLSv1.2
spring.datasource.username=QSKxMfLGTtKmExJ.root
spring.datasource.password=3PBlfyMMp5cOb78U
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Security
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=ADMIN

# Static resources
spring.web.resources.static-locations=classpath:/static/
spring.security.ignored=/favicon.ico,/css/**,/js/**,/images/**,/login,/auth/**

# JPA

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hikari
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Server
debug=true
server.port=8080

# Stripe
stripe.api-key=${STRIPE_API_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}

# Multipart (??? 413)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
server.tomcat.max-http-form-post-size=5MB

spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true